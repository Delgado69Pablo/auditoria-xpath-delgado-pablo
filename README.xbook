# Refactorización XSD – TechNova Cloud v2.0

## a) Líneas de código ahorradas usando grupos

- Eliminación de atributos repetidos en `<servidor>` gracias a `attributeGroup`: **–9 líneas**
- Sustitución del bloque completo de `<hardware>` por un `group`: **–28 líneas**

**Total aproximado ahorrado: 37 líneas**

## b) Error que da VS Code al duplicar IDs

Al intentar usar dos servidores con el mismo atributo `id`, VS Code muestra:

